{
  "name": "mobile-app",
  "version": "0.0.0",
  "description": "Cool Culture Mobile App ",
  "scripts": {
    "postinstall": "cd angular && npm config set strict-ssl false && npm install && bower install"
  },
  "dependencies": {
    "grunt": "~0.4.4",
    "grunt-hub": "^0.6.2",
    "bower": "^1.3.1",
    "eventemitter3": "^0.1.6"
  }
}
