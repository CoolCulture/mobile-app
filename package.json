{
  "name": "mobile-app",
  "version": "0.0.0",
  "description": "Cool Culture Mobile App ",
  "scripts": {
    "postinstall": "cd angular && npm install && bower install"
  },
  "dependencies": {
    "grunt": "~0.4.1",
    "grunt-hub": "^0.6.2"
  }
}
