<li>
  <%= activity.name %> <%= "- #{activity.date}" if activity.date %>
  <%= ", #{activity.start_time} - #{activity.end_time}" if activity.start_time %>
  <p><%= activity.description %></p>
  <%= link_to 'Edit', edit_admin_museum_activity_path(activity.museum, activity) %>
  <%= link_to 'Delete', admin_museum_activity_path(activity.museum, activity), method: :delete, confirm: "Really delete #{activity.name}?" %>
</li>