<li>
  <%= activity.name %> - <%= activity.description %>
  <%= link_to 'Edit', edit_museum_activity_path(activity.museum, activity) %>
  <%= link_to 'Delete', museum_activity_path(activity.museum, activity), method: :delete, confirm: true %>
</li>
